# Production Environment Configuration Template
# This file shows the required environment variables for production
# DO NOT commit actual production values to version control
# Production secrets should be retrieved from AWS Secrets Manager

# Application Configuration
NEXT_PUBLIC_ENV=production
NEXT_PUBLIC_APP_URL=https://your-production-domain.com
NEXT_PUBLIC_API_URL=https://your-production-domain.com/api

# AWS Configuration (Set by deployment environment)
NEXT_PUBLIC_AWS_REGION=us-east-2
AWS_REGION=us-east-2
# AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY should be set by deployment platform
# These are used to access AWS Secrets Manager for other secrets

# AWS Cognito Configuration (fallback values, production uses Secrets Manager)
NEXT_PUBLIC_COGNITO_USER_POOL_ID=YOUR_PRODUCTION_USER_POOL_ID
NEXT_PUBLIC_COGNITO_USER_POOL_CLIENT_ID=YOUR_PRODUCTION_CLIENT_ID
COGNITO_USER_POOL_DOMAIN=your-production-domain

# Card Data APIs (production URLs)
YGO_PRO_API_URL=https://db.ygoprodeck.com/api/v7
POKEMON_TCG_API_URL=https://api.pokemontcg.io/v2
SCRYFALL_API_URL=https://api.scryfall.com
API_TCG_URL=https://api.apitcg.com/v1
# API keys are retrieved from AWS Secrets Manager

# Feature Flags
ENABLE_CARD_DATA_APIS=true
ENABLE_REAL_AUTHENTICATION=true
ENABLE_AWS_SES=true
ENABLE_EMPLOYEE_INVITATION=true
ENABLE_REACT_QUERY_DEVTOOLS=false

# Rate Limiting & Security (production values)
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX_REQUESTS=1000
CORS_ORIGIN=https://your-production-domain.com

# Monitoring
NODE_ENV=production

# Note: All sensitive values (API keys, database credentials, etc.) 
# are retrieved from AWS Secrets Manager in production
# See AWS_SECRETS_SETUP.md for configuration details